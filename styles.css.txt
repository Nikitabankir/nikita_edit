* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    min-height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    overflow: hidden;
    font-family: Arial, sans-serif;
    color: white;
    background:
        radial-gradient(circle at 20% 20%, #3b176b 0, transparent 35%),
        radial-gradient(circle at 80% 80%, #72184f 0, transparent 35%),
        #090611;
}

.card {
    position: relative;
    width: min(90%, 600px);
    padding: 55px 35px;
    text-align: center;
    border: 1px solid rgba(255, 255, 255, .15);
    border-radius: 30px;
    background: rgba(255, 255, 255, .07);
    backdrop-filter: blur(18px);
    box-shadow: 0 25px 80px rgba(0, 0, 0, .5);
    animation: appear 1s ease;
}

.cake {
    font-size: 65px;
    animation: float 3s ease-in-out infinite;
}

.small-text {
    margin-top: 15px;
    color: #ffca6b;
    font-size: 14px;
    text-transform: uppercase;
    letter-spacing: 3px;
}

h1 {
    margin-top: 15px;
    font-size: clamp(38px, 8vw, 65px);
    line-height: 1.05;
}

h1 span {
    color: #ffbd59;
    text-shadow: 0 0 25px rgba(255, 189, 89, .5);
}

p {
    max-width: 480px;
    margin: 25px auto;
    color: #d8d3df;
    font-size: 17px;
    line-height: 1.7;
}

.gift {
    font-size: 70px;
    margin: 10px 0 25px;
    animation: giftFloat 2s ease-in-out infinite;
}

.gift-button {
    display: inline-block;
    padding: 16px 32px;
    border-radius: 50px;
    color: #211304;
    background: linear-gradient(135deg, #ffd36a, #ff9f43);
    text-decoration: none;
    font-size: 18px;
    font-weight: bold;
    box-shadow: 0 10px 30px rgba(255, 170, 60, .3);
    transition: .25s;
}

.gift-button:hover {
    transform: translateY(-4px) scale(1.03);
    box-shadow: 0 15px 40px rgba(255, 170, 60, .5);
}

.footer {
    margin-top: 30px;
    color: #aaa2b5;
    font-size: 14px;
}

@keyframes appear {
    from {
        opacity: 0;
        transform: translateY(30px) scale(.96);
    }

    to {
        opacity: 1;
        transform: translateY(0) scale(1);
    }
}

@keyframes float {
    0%, 100% {
        transform: translateY(0);
    }

    50% {
        transform: translateY(-10px);
    }
}

@keyframes giftFloat {
    0%, 100% {
        transform: translateY(0) rotate(-3deg);
    }

    50% {
        transform: translateY(-8px) rotate(3deg);
    }
}

@media (max-width: 500px) {
    .card {
        padding: 40px 22px;
    }

    p {
        font-size: 15px;
    }
}